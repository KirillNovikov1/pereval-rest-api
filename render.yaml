services:
  - type: web
    name: pereval-rest-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    healthCheckPath: /health
    autoDeployTrigger: off
    envVars:
      - key: FSTR_DB_URL
        sync: false
